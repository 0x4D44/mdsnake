# JRN — Coil implementation (ultracode, full-game build)

**Date:** 2026-06-20
**Branch:** `task/coil-impl` (worktree `C:/worktrees/mdsnake/coil-impl`, off `origin/main` @ 5e13d31)
**HLD:** `wrk_docs/2026.06.20 - HLD - coil-snake-puzzle-game.md` (Status: SIGNED-OFF) ✓ precondition met.

## Kickoff decisions (Arthur, AskUserQuestion)

- **Scope: ATTEMPT THE FULL GAME** (Inc 1–4, all worlds/abilities) — overriding my
  Inc-1-only recommendation.
- **Almanac: ALSO DEPLOY** — build dist, integrate into `0x4d44.github.io` (coil/ +
  data.js [DRAFT] entry + ill-coil), and push to main (live Pages deploy).

## Team-lead decisions (autonomous, recorded per the brief)

- **D-IMPL-1: Honour the HLD's increment sequencing as the build order** (Inc 1→2→3→4),
  committing each as a complete passing unit. The core (`types.ts`/`game.ts`/tests) is a
  single evolving module touched by every increment → core evolution is SEQUENTIAL;
  parallelism is used only for genuinely non-colliding files (rooms, renderer).
- **D-IMPL-2: D24 human playtest cannot run autonomously.** Rooms will be authored
  SOLVABLE (machine-checked by recorded `(verb,dir)[]` solutions via T-ROOM-SOLVE) but
  are UN-PLAYTESTED for fun/difficulty. Flagged for Arthur; not a build blocker given
  the full-game directive.
- **D-IMPL-3: Spike-before-content** (anchor/carry/co-op) satisfied as inline validation
  tests proving the mechanic on a minimal config before authoring that world's rooms,
  rather than literal throwaway rooms.
- **D-IMPL-4: Constrict stays CUT** (HLD D29); wrap is emergent body-blocking only.

## Phases (one workflow per phase, reviewed between)
1. VALIDATE & DRIFT — confirm scaffold matches HLD "current behavior" + impact map;
   extract acceptance criteria as the definition of done.
2. PLAN — work items, file partitions, ordering.
3. IMPLEMENT — sequential increment pipeline, tests-first, commit per passing unit.
4. REVIEW — multi-dimension + simplify, adversarial verification, loop until clean.
Then almanac build + deploy.

---

## Phase log

### Phase 5 — Finalize: review loop closed, version bump, integrate (in progress)
- **Review round 2** (`coil-review-r2`, 3 lenses + verify): **0 confirmed blocker/major**;
  6 minors. Applied the two cheap high-value ones myself: scoring.ts now imports the
  kernel's canonical `allBodies` (removes the round-1-style dup it had reintroduced) and
  dropped two tautological `maxLength` asserts in scoring.test.ts (the won-trace pair is
  the real teeth). Gates green (550/550, tsc). **D-IMPL-19:** review loop closed here —
  round 1 substantive, round 2 clean of confirmed issues; a 3rd round is diminishing
  returns against the 550-oracle standing guard (proportionality). Remaining minors
  (World-6 negative could pass for the wrong reason; per-world test-harness DRY) left as
  borderline/optional per the simplify discipline.
- **Version bump:** 0.1.0 → 0.2.0 (ONE minor bump for the whole task; the full game is a
  large feature addition — semver minor, pre-1.0).

### Phase 4 — REVIEW round 1 + fixer (done, committed)
- `coil-review` (6 lenses → 2-of-3 verify, 36 agents): 10 confirmed (incl. the renderer
  co-op blocker found by 5 lenses), 11 minors. `coil-review-fix` (3 stages) applied them:
  **typecheck clean, 550/550 tests, build OK.**
- Render: `drawItems(state)` pure seam + draw ALL bodies via `allBodies()`; new
  `draw.test.ts` oracle (per-coordinate, teeth-tested); colour-literal dedup; fixed the
  stale `game.legacy.ts` header (KEEP — it's the live T-RESOLVE-EQUIV comparand).
- Scoring: co-op fix (hidden-egg/peak-length iterate all bodies) + co-op oracle; `store`
  save/load guarded against throwing/tampered storage.
- Tests: shared `bfs.ts`; **par-minimality** (`bfs===par`) added to W1-5; **load-bearing
  negative oracles** added where BFS confirms the mechanic is required; W1-5 solution
  duplication removed (import from `solutions.ts`).
- **D-IMPL-18 (honest oracle finding → scratchpad + report):** BFS proved several rooms
  are *bypassable* (W1 r1/r5/r6/r7 walkable without strike; W2 r1/r5 without growth; W5
  r5 walkable). The fixer guarded ONLY the genuinely load-bearing rooms and asserted the
  strongest TRUE claim elsewhere (`bfs<=par` for W1 r1/r6/r7 per D15 author-asserted par;
  exact `bfs===par` for all other W1-5). The "loose" rooms are SOLVABLE but don't force
  their taught mechanic — a level-design/playtest item (D24), parked in `scratchpad.md`.
- Renderer KIND_STYLE/`allBodies` etc. confirmed correct; gripBeside/egg-coordinate/
  decoy-on-plate confirmed acceptable documented resolutions (no change).

### Phase 3 — IMPLEMENT Increment 4b (done, committed) — implementation feature-complete
- Workflow `coil-impl-inc4b` (co-op core → World 7 + Finale W8). Re-verified:
  **typecheck clean, 509/509 tests, build OK.**
- Co-op: `bodies?: Segment[][]` (M2 invariant: single-snake keeps bodies undefined);
  settle rewritten as the §2.5 general transitive-grounding **simultaneous fixpoint**
  over `[snake, ...(bodies??[])]` — implemented as two explicit branches (single-body =
  literal original loop for trivially-reviewable byte-identity; multi-body = the closure).
  Verbs act on the active body; Tab cycles; checkEnd = all-heads-win, any-void-death,
  **win-before-death** (D28). Oracles T-COOP-MIGRATE/1/2, T-COOP-WIN-PRECEDENCE,
  T-SETTLE-MUTUAL/CONVERGE, SPIKE-I4b. **No existing oracle weakened**; T-RESOLVE-EQUIV
  stayed green (the byte-identity proof for the settle rewrite).
- World 7 "Two Bodies" (4 rooms, BFS-verified unsolvable without Tab; body-as-platform
  realised as the HLD's "each body holds a gate" relay) + Finale W8 "Recombination"
  (3 rooms, shipped abilities only, each load-bearing). 14 worlds-worth of content:
  Worlds 1-8, ~38 rooms total, every room machine-solvable with honest BFS-optimal par.
- **Autonomous decisions / flags (recorded):**
  - **D-IMPL-15 (must-fix before deploy):** renderer draws only `state.snake`, not
    `state.bodies` → live W7 rooms show only the active body (sim correct & verified;
    visual only). Small fix (iterate `[snake,...bodies]`). **Assigned to the REVIEW phase**
    (which edits code) before the almanac deploy.
  - **D-IMPL-16:** W7 "body-as-platform" built as the HLD's stated alternative ("each
    body holds a gate" via stacked plate/gate relays) — orthogonal-grid movement made
    literal walk-across-the-other-body's-back puzzles fragile; gate-relay is clean,
    legible, and BFS-proven co-op. SPIKE-I4b still covers strike-off-another-body's-back.
  - **D-IMPL-17:** `RoomMeta.build?()` + `coop?` + uniform `buildRoom(meta)` accessor in
    worlds.ts express co-op rooms without changing core `buildState`. Two out-of-partition
    edits (main.ts → buildRoom; scoring.test.ts T-PAR → buildRoom) — both required and
    oracle-STRENGTHENING (T-PAR now builds co-op rooms correctly).

### Phase 3 — IMPLEMENT Increment 4a (done, committed)
- Workflow `coil-impl-inc4a` (carry/decoy → scoring/store/shell + World 6). Re-verified:
  **typecheck clean, 432/432 tests, build OK.**
- Carry: `pickup` flag + frozen `object` preset; `carry?` on Segment; eating a pickup
  stores it on the head (length unchanged); pure deposit verb (registered with battery);
  decoy = deposit a body-shaped solid. T-CARRY-1/2, T-DECOY (static-structure half),
  P-CONSERVATION/P-UNDO extended to carry. Carry-forward bug (gut sliding off tail)
  caught by the property battery and fixed; T-RESOLVE-EQUIV stays green.
- Scoring (OUTSIDE the core): `scoring.ts` eggs (solve/hidden-positional/constraint
  maxMoves|maxLength|noStrike) + `store.ts` injected localStorage wrapper; T-EGG-HIDDEN,
  T-EGG-CONSTRAINT, T-PAR (all rooms, cross-increment), T-PERSIST, T-GATING (solve-eggs
  unlock + skip-after-K). World 6 "The Gullet" (6 rooms, BFS-verified carry-gated).
- **Autonomous decisions / deviations (recorded):**
  - **D-IMPL-10:** hidden egg implemented as a scoring-only coordinate (`RoomMeta.eggAt`,
    tracked from the trace) rather than a core `egg` Entity flag — zero core change (the
    task's preferred path). If a future world needs an on-board egg entity, add the flag
    then.
  - **D-IMPL-11:** T-PERSIST exercised via an injected in-memory `StorageLike` (node env,
    no jsdom; store is injected by design → logic is window-independent). main.ts wires
    the real `window.localStorage`.
  - **D-IMPL-12 (LIMITATION → Arthur):** T-DECOY "holds a plate" (literal) is NOT
    buildable in the current model — a cell holds ONE entity and the mechanism pass keys
    occupancy off snake SEGMENTS, not deposited blocks. Decoy-as-static-structure
    (supports/blocks) works and is pinned; decoy-on-plate would need a layered-cell model
    or block-counts-for-triggers rule — a design change beyond this run's authority.
    W6 rooms use decoy-as-structure only. **Flagged for an HLD decision.**
  - **D-IMPL-13:** `object` preset is pickup+solid → an empty-gut snake re-swallows a
    deposited block (pickup precedence); a block only blocks a full-gut snake. Consistent
    & arguably useful; a permanent decoy would need a non-pickup variant.
  - **D-IMPL-14:** `solutions.ts` introduced as a cross-room T-PAR source; Worlds 1-5
    solutions are duplicated there (their rooms.test.ts keep own copies for load-bearing
    guards). Intentional minor duplication → flagged for the REVIEW/simplify pass.

### Phase 3 — IMPLEMENT Increment 3 (done, committed)
- Workflow `coil-impl-inc3` (pipeline → mechanisms → Dark). Re-verified by me:
  **typecheck clean, 322/322 tests, build OK** (>500 kB = informational Vite advisory;
  M-BUNDLE non-breaking, §4.7).
- Pipeline: frozen `game.legacy.ts` (verbatim pre-Inc3 kernel) as OLD comparand;
  `resolve(s)=checkEnd(applyMechanisms(settle(s)))`; status-monotonicity (D28) — all
  three passes early-return same ref on terminal status; `buildState` now routes through
  the full tail (F10/G7) so resting-on-exit builds `won` (Inc-1 limitation test updated
  to the intended new behaviour — allowed rule change per the par-stability policy).
  fast-check ^4.8 added test-only; **T-RESOLVE-EQUIV** = whole-verb OLD-vs-NEW deep-equal
  over a seeded Arbitrary + 15 pinned adversarial seeds; **mutation-tested** (dropping
  pre-resolve checkWin turned it red → oracle has teeth).
- Mechanisms: `trigger`/`door` flags + frozen plate/gate presets + `triggers` set;
  `applyMechanisms` = `gate.solid = !(door∈triggers) && !occupied`, pure, no-latch,
  same-ref fast path (preserves no-op identity); **M1 invariant** enforced (gate has
  solid, not supports → single-pass settle safe). T-MECH-1/2/3, T-BUILD mechanism case,
  T-ASCII two-pair distinct ids. World 4 "Pressure" (5 rooms, gates proven load-bearing).
- Dark: `heat` flag + heatlamp preset; renderer dark mode (M-DARK manual); **core heat
  regression test** proves heat is inert to rules. World 5 "Dark" (rooms; same rules,
  visibility only).
- **D-IMPL-9:** kept `game.legacy.ts` + T-RESOLVE-EQUIV in-tree as the Inc-3 proof
  (HLD allows deleting post-green; the simplify/REVIEW phase will decide whether to
  prune it — it's a frozen ~100-line reference + a teeth-having oracle, low cost).

### Phase 3 — IMPLEMENT Increment 2 (done, committed)
- Workflow `coil-impl-inc2` (anchor mechanic + spike → Worlds 2-3). Re-verified by me:
  **typecheck clean, 126/126 tests, build OK.**
- Anchor: `Segment` widened to `{x,y,anchored?}`; `grip` flag + frozen `anchor` preset;
  pure `anchor(s)` verb (registered with the property battery); grounding derived from
  grip each turn, no latch (D25); settle stays the single-body loop + one added
  grounding source (NOT the multi-body fixpoint — D30 honoured). Oracles T-ANCHOR-1/2/
  NOOP/CARRY, T-CLIMB, and SPIKE-I2a (spike-before-content gate) all green.
- Worlds 2 (Growth: Reach/Bridge/Curl/Restraint-twist[constraint egg]/Graduation) and
  3 (The Climb: First Grip/Ascent/Traverse/Release-twist/Graduation), 10 rooms total,
  each BFS-verified solvable with honest BFS-optimal par; W3 rooms additionally
  BFS-verified UNSOLVABLE without the anchor verb.
- **Autonomous decision D-IMPL-8 (resolved HLD ambiguity):** the HLD says a segment
  grounds while it "sits on a grip cell", but the `anchor` preset is `solid` — a segment
  can never occupy a solid cell, so "on a grip cell" is literally unsatisfiable. Adopted
  the only climbable reading consistent with "climb ALONG a grip wall": a segment grips a
  grip surface **orthogonally adjacent** to it (`gripBeside`). A snake grips a wall it is
  beside, not one it stands on. All anchor oracles authored against this model. (If a
  literal occupiable non-solid grip cell was intended, drop `solid` from the preset and
  read `cellAt(seg).grip` — noted for any future revisit; the adjacent model is the
  natural one for a climbing snake.)

### Phase 3 — IMPLEMENT Increment 1 (done, committed)
- Workflow `coil-impl-inc1` (3 sequential stages). Independently re-verified by me:
  **typecheck clean, 74/74 tests pass, vite build OK** (bundle ~494 KB / 125 KB gz).
- Core: `CellType` union → `Entity{kind,solid?,supports?,eat?,win?}`; runtime-frozen
  `PRESETS` is the only construction path; `LevelDef` authors by preset NAME so the 9
  slice tests stayed byte-identical green (SLICE-REGRESSION). Property battery
  (P-DETERMINISM/PURITY/NOOP-IDENTITY/UNDO/CONSERVATION/SETTLE-TERMINATION) over a
  13-state adversarial corpus + verb-registration; T-PRESETS (Object.freeze); all gap
  oracles (T-STRIKE-3/4/5/NOOP, T-VOID-PARTIAL, T-EAT-SELF, T-BUILD, T-TERMINAL,
  T-WIN-FALL/STRIKE-CROSS/STRIKE-VOID).
- Content: pure `ascii.ts` (glyphs + per-room legend; snake via ordered digits 0=head;
  bottom row = floorY) + T-ASCII; `worlds.ts` registry; World 1 R1-R7 each
  machine-solvable via recorded `(verb,dir)[]` replay (T-ROOM-SOLVE) with par + an
  honest-par guard; hidden-egg/constraint metadata seeded.
- Shell: `main.ts` parallel `inputs[]` log (lockstep with undo, unbounded, reset on
  restart/nav — closes open-Q#7), record-par key, room nav; `renderer.ts` KIND_STYLE
  table + camera auto-fit once on room load from authored bounds.
- **Autonomous decisions (recorded):**
  - **D-IMPL-6:** T-BUILD "head-on-exit builds won" pinned to ACTUAL current behaviour
    — resting-on-exit stays `play` at Inc 1 (checkWin only runs inside settle's fall
    loop; no `checkEnd` until Inc 3 per F10/§4.3). Win-on-fall-to-exit works. The
    checklist line was slightly ahead of the increment; resting-on-exit-win lands at
    Inc 3 with buildState-via-resolve. No code forced. Consistent with the HLD body.
  - **D-IMPL-7:** version bump deferred to ONE bump at end-of-task (per Version policy:
    one bump per integrated task, not per intermediate increment commit).
  - Renderer got a 3-line refactor in the core stage (cells values are now `Entity`)
    — required for the workspace tsc gate; reads `kind` exactly as the HLD specifies.

### Phase 2 — PLAN (done; collapsed inline per proportionality)
- **D-IMPL-5:** PLAN run inline (not a workflow) — the work-item breakdown is fully
  determined by the HLD's increment structure, which I authored. Journaled here.
- Core (`types.ts`/`game.ts`/`game.test.ts`) is one evolving module → sequential
  increment implementers; one implementer per increment (mechanic + oracles + that
  world's rooms together) to avoid concurrent edits to the core. I (lead) run the
  gates and commit each passing unit; agents do NOT commit.
- **Refactor guidance (load-bearing):** the entity/PRESETS refactor keeps `LevelDef`
  authoring via a preset-NAME `type` field (`buildState` maps `type -> PRESETS[type]`),
  so the existing behavioural tests stay green unchanged (SLICE-REGRESSION oracle).
  Internal `cells` becomes `Map<string,Entity>`; rules read flags.
- Work items / order:
  - **WI-1 (Inc1-core):** harden FIRST (property battery P-* + committed adversarial
    corpus + verb-registration + gap tests T-STRIKE-3/4/5/NOOP, T-VOID-PARTIAL,
    T-EAT-SELF, T-BUILD, T-TERMINAL, T-WIN-*) GREEN on pre-refactor code; THEN
    entity/PRESETS refactor + T-PRESETS; slice regression green.
  - **WI-2 (Inc1-shell/content):** ascii.ts (+legend, T-ASCII), worlds.ts, World 1
    rooms R1-R7 (+par/egg, T-ROOM-SOLVE replays recorded (verb,dir)[]), main.ts
    (input-log + record-par + unbounded undo + room nav), renderer (entity.kind +
    camera auto-fit on load). → commit Inc 1.
  - **WI-3 (Inc2):** grip/anchored + anchor verb + grounding(anchored&&on-grip) +
    spike test; Worlds 2-3 rooms. → commit.
  - **WI-4 (Inc3):** resolve-tail extraction + frozen game.legacy.ts + T-RESOLVE-EQUIV
    + fast-check dep; trigger/door/heat + applyMechanisms + buildState-via-resolve +
    M1 no-support guard; Worlds 4-5 (dark = renderer heat mode). → commit.
  - **WI-5 (Inc4a):** pickup/carry/deposit + decoy + egg + scoring (eggs/par/constraint
    + localStorage progress); World 6. → commit.
  - **WI-6 (Inc4b):** bodies[] + multi-body simultaneous-fixpoint settle + Tab +
    win-before-death + T-COOP-MIGRATE golden trace; World 7 + Finale W8. → commit.
  - **REVIEW** (multi-dimension + simplify, adversarial verify, loop to clean).
  - **WI-7:** almanac build + deploy (coil/ dist, data.js [DRAFT] entry, ill-coil,
    CLAUDE.md mapping, SOURCE.txt SHA) + push to main.

### Phase 1 — VALIDATE & DRIFT (done)
- Workflow `coil-validate` (2 readers): verdict **MATCHES**, 0 fundamental conflicts;
  2 trivial forward-describing notes (buildState is plain settle pre-Inc3; vite config
  is the combined vitest/config — both already consistent, no adaptation).
- 61 acceptance criteria extracted → `.impl-work/acceptance-criteria.md` (definition
  of done). Manual-only: M-DARK, M-RENDER, M-LOAD, M-LISTING, **D24-PLAYTEST**
  (the human gate — can't run autonomously; see D-IMPL-2).

### Phase 0 — Worktree bring-up (done)
- `task/coil-impl` worktree cut off origin/main @ 5e13d31; npm install OK.
